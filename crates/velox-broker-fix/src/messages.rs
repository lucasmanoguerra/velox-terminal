//! FIX message encoding and decoding.

/// Placeholder for FIX messages.
pub struct FixMessages;

impl Default for FixMessages {
    fn default() -> Self {
        Self::new()
    }
}

impl FixMessages {
    pub fn new() -> Self {
        Self
    }
}
