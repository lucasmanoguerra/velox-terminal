//! FIX TCP transport layer.

/// Placeholder for FIX transport.
pub struct FixTransport;

impl Default for FixTransport {
    fn default() -> Self {
        Self::new()
    }
}

impl FixTransport {
    pub fn new() -> Self {
        Self
    }
}
