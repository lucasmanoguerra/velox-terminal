//! FIX session lifecycle management.

/// Placeholder for FIX session.
pub struct FixSession;

impl Default for FixSession {
    fn default() -> Self {
        Self::new()
    }
}

impl FixSession {
    pub fn new() -> Self {
        Self
    }
}
