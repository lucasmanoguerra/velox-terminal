//! Backtesting metrics — Sharpe, Sortino, drawdown, etc.

/// Placeholder for backtesting metrics.
pub struct BacktestMetrics;

impl Default for BacktestMetrics {
    fn default() -> Self {
        Self::new()
    }
}

impl BacktestMetrics {
    pub fn new() -> Self {
        Self
    }
}
