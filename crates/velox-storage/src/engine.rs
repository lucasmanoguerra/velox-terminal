//! Storage engine — time-series data storage.

/// Placeholder for storage engine.
pub struct StorageEngine;

impl Default for StorageEngine {
    fn default() -> Self {
        Self::new()
    }
}

impl StorageEngine {
    pub fn new() -> Self {
        Self
    }
}
