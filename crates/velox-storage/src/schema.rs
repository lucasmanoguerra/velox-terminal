//! Storage schema — table definitions, partitioning.

/// Placeholder for storage schema.
pub struct StorageSchema;

impl Default for StorageSchema {
    fn default() -> Self {
        Self::new()
    }
}

impl StorageSchema {
    pub fn new() -> Self {
        Self
    }
}
